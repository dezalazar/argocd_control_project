apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: helm-app-prd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/dezalazar/argocd_helm_project.git'
    path: charts/hello-world      # La ruta donde está tu Chart.yaml
    targetRevision: main
    helm:
      valueFiles:
        - values-prd.yaml         # El archivo de configuración específico para PRD 
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: prd 
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
